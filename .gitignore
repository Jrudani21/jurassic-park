# Secrets & local config — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# Agent runtime output (regenerable — never commit)
meeting_notes.md
agents/*/output/
agents/*/*.log
data/

# Node (installed for tooling, not part of project)
node_modules/
package-lock.json
package.json

# LLM response cache (runtime)
agents/_shared/.llm_cache/
agents/*/demo.sqlite
